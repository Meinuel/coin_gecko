import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        height: 40,
        borderStyle: "solid",
        borderWidth: 0.5
    },
});

export default styles;