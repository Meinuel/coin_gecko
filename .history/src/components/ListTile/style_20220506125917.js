import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        height: 40,
        borderStyle: "solid",
        borderWidth: 0.25,
        alignItems: "center"
    },
});

export default styles;