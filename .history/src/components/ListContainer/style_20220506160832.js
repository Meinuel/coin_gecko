import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    filtersContainer: {
        display: "flex",
        flexDirection: "row",
    },
    coinsContainer: {
        flex: 1,
        width: '100%'
    }
})

export default styles;